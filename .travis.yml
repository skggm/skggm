language: python
python: 2.7

sudo: false

# Pre-install packages for the ubuntu distribution
cache:
    apt: true
    directories:
        - $HOME/.cache/pip

addons:
    apt:
        packages:
            - libblas-dev
            - liblapack-dev
            - libatlas3gf-base
            - libatlas-dev

python:
    - 2.7
    - 3.6

# command to install dependencies
install:
    - pip install -U pip wheel
    - pip install --install-option="--no-cython-compile" cython
    - pip install -r requirements.txt
    - python setup.py -q install

# command to run tests
script: flake8 inverse_covariance
script: python -m pytest --showlocals --pyargs
